# V12.02 DOG CLEARANCE — TEST 10

Baseline: V12.01 TEST 7 (user-tested PASSED). TEST 8/9 were not treated as the baseline.

Requested Capture-screen changes implemented:
- Existing-client selection hides the telephone/new-number capture section.
- Existing-client selection hides SAVE / UPDATE.
- RETRIEVE LIST remains available when matching contacts exist and after selection.
- CLEAR is visible when matching contacts are present and when an existing client is selected/retrieved.
- CLEAR remains available when RETRIEVE LIST is used to show the list.
- UNDO is retained.
- Delete/trash controls added to the first and second fixed telephone rows; they clear those row values without removing the rows.
- Existing extra capture-number rows retain their existing delete controls.

Static checks completed: brace and parenthesis balance; required symbols/controls present.

Not device-tested and not Gradle-built in this environment. TEST 7 PASSED remains the approved baseline until TEST 10 is tested on the user's device.
