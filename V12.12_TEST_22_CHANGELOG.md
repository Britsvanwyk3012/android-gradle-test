# V12.12 — TEST 22 CHANGELOG

## Purpose
Correct the confirmed telephone-number deletion failure identified during TEST 21.

## Changes
- Removed the live-phone merge behaviour from the Google People API edit path. The submitted Buy & Sell phone list is now the desired list, so numbers deliberately deleted in the app are not silently re-added.
- Removed the live-phone merge behaviour from the Android ContactsContract update path.
- Android phone rows are replaced using the current desired local phone list rather than preserving previously existing external numbers.
- Retained the existing Google/Android contact lookup and number normalisation logic.

## Scope control
- This version focuses on the confirmed deletion/synchronisation issue.
- Duplicate-number warning design and CANCEL → UNDO improvement remain recorded for a later implementation pass unless separately added after source review.

## Verification status
- Source patch applied.
- No Gradle build performed in this environment because external Gradle distribution access is unavailable.
- Device verification required in Android Studio and on the Samsung phone.
