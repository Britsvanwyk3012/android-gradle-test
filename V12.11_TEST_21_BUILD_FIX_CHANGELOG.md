# V12.11 — TEST 21 CHANGELOG

Date: 2026-09-17

## Purpose
Correct the TEST 20 build failure reported by Android Studio.

## Fix implemented
- Added a `normalizePhone(value: String)` helper inside the nested `ClientDb` class.
- `ClientDb.findPossibleDuplicate(...)` and its internal phone comparisons can now resolve `normalizePhone()` within their own scope.
- The existing `MainActivity` helper was retained for the activity-level code that already uses it.

## Verification
- Source inspection confirms the previously unresolved calls inside `ClientDb` now have an in-scope helper.
- A local Gradle build was attempted, but the environment could not download Gradle 9.5.0 because `services.gradle.org` was unreachable. Therefore, this package is **not build-verified** here.
- No Android device testing has been performed.

## Status
TEST 21 is a source correction package only. Build success, installation, device testing, clearance, and approval remain outstanding until Brits tests it in Android Studio and on the phone.
